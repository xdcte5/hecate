# Routing is ability-based via harness cards in registry.yaml.
routing: []

failover:
  - pi
  - cursor
  - claude-code
  - codex

governance:
  requireGitSnapshotOnHandoff: true
  maxHandoffTokens: 8000
  maxTranscriptLines: 200
