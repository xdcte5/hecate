---
name: run-tests
description: Run the project's vitest suite and summarize failures.
---

Run `pnpm test`. If anything fails, summarize the failing test names and the first
assertion error for each. Do not attempt fixes unless asked.
